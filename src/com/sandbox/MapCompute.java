/*
 * Copyright (C) Coriant
 * The reproduction, transmission or use of this document or its contents 
 * is not permitted without express written authorization.
 * Offenders will be liable for damages.
 * All rights, including rights created by patent grant or 
 * registration of a utility model or design, are reserved.
 * Modifications made to this document are restricted to authorized personnel only. 
 * Technical specifications and features are binding only when specifically 
 * and expressly agreed upon in a written contract.
 *
 */

package com.ossnms.sandbox;

/**
 * Created by pt102933 on 2/2/2017.
 */
public class MapCompute {

    public static void main(String[] args) {
/*
        AtomicInteger atomicInteger = new AtomicInteger(0);

        ObservableMap<Integer, List<Integer>> map = new ObservableMapWrapper<>(new HashMap<>());
        map.put(1, new ArrayList<>());

        System.out.println(atomicInteger.get());

        map.addListener(new MapChangeListener<Integer, List<Integer>>() {
            @Override
            public void onChanged(Change<? extends Integer, ? extends List<Integer>> change) {
                atomicInteger.getAndIncrement();
            }
        });

        map.get(1).add(1);
        map.put(1, null);

        System.out.println(atomicInteger.get());
        */
    }
}
